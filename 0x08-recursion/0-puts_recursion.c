#include "main.h"
/**
 * _puts_recursion - Function to use
 * @s: String to print
 * Description: Function to print a string
 * Return: String
 */
void _puts_recursion(char *s)
{
	while (*s != '\0')
		s++;
	return (0);
}
