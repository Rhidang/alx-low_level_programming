#include "main.h"
/**
 * _strlen - Function to use
 * @s: String to check
 * Description: To check the length of a string
 * Return: Always zero
 */
int _strlen(char *s)
{
	char s;
	int len;

	len = strlen(s);
}
