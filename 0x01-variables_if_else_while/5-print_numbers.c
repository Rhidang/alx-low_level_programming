#include <stdio.h>
/**
 * main - Entry point
 * Description: Print all single digit numbers in base 10
 * Return: Always 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)

	{
		printf("%i", n);
	}
	printf('\n');
	return (0);
}
