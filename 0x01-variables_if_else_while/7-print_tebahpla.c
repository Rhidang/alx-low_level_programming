#include <stdio.h>
/**
 * main - Entry point
 * Description: Prints alphabets backwards
 * Return: Always 0
 */
int main(void)
{
	char mirror = 'z';
	
	while mirror >= 'a';

	{
		putchar(mirror);
		mirror--;
	}
	putchar('\n');
	return (0);
}
